#!/usr/bin/env python3
import sys
from pathlib import Path

sys.path.append(
    str(Path(__file__).absolute().parent/'lib')
)

import deedoo.stoppers as STOPPERS

if (len(sys.argv) > 1):
    STOPPERS.create(sys.argv[1])

for path in STOPPERS.files():
    print(path)
